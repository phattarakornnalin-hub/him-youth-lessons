# Admin Panel — คู่มือการใช้งาน

## เข้าถึง Admin Panel

เปิดไฟล์ `admin.html` ในเบราว์เซอร์ของคุณ

```
http://localhost:8000/admin.html  (ถ้าใช้ local server)
```

---

## หน้า Admin Panel ประกอบด้วย

### 1. **ส่วนสร้างบทเรียนใหม่** (ด้านบน)
ฟอร์มสำหรับเพิ่มบทเรียนใหม่

### 2. **รายการบทเรียน** (ด้านล่าง)
แสดงบทเรียนทั้งหมดที่เก็บไว้พร้อมปุ่มแก้ไขและลบ

### 3. **ปุ่มการจัดการ** (ที่ header)
- 📥 **นำเข้า JSON** — เพื่ออนุญาต manifest.json เดิม
- 📤 **ส่งออก JSON** — ดาวน์โหลดไฟล์ที่เตรียม deploy
- 👁️ **ดูเว็บไซต์** — ดูเว็บไซต์ที่เผยแพร่

---

## วิธีการสร้างบทเรียนใหม่

### ขั้นตอนที่ 1: กรอกข้อมูล

| ฟิลด์ | ตัวอย่าง | หมายเหตุ |
|------|--------|---------|
| **ID** | `0004-my-lesson` | URL-safe (อักษรตัวเล็ก ตัวเลข ยติภาค) |
| **ชื่อบทเรียน** | `ผู้นำที่รับใช้` | ชื่อแสดงในเว็บไซต์ |
| **หมวดหมู่** | `การเป็นผู้นำ` | เลือกจากรายการหรือสร้างใหม่ |
| **วันที่** | `2026-10-15` | วันตีพิมพ์บทเรียน |
| **สรุปสั้น** | `บรรยายเกี่ยวกับ...` | แสดงในรายการบทเรียน |
| **เนื้อหา** | Markdown text | ดูรายละเอียด Markdown ด้านล่าง |

### ขั้นตอนที่ 2: เขียนเนื้อหา (Markdown)

ใช้ Markdown syntax ธรรมชาติ:

```markdown
# ชื่อบทเรียน

กรรมการแนะนำ...

## หัวข้อที่ 1

เนื้อหา...

- จุดสำคัญที่ 1
- จุดสำคัญที่ 2

> เป็นคำพูดหรือข้อคิด

### หัวข้อย่อย

เนื้อหาเพิ่มเติม...
```

### ขั้นตอนที่ 3: บันทึก

คลิกปุ่ม **💾 บันทึกบทเรียน**

บทเรียนจะปรากฏในรายการด้านล่างทันที

---

## วิธีแก้ไขบทเรียน

1. ค้นหาบทเรียนในรายการด้านล่าง
2. คลิกปุ่ม **✏️ แก้ไข**
3. ข้อมูลจะเต็มลงในฟอร์มด้านบน
4. เปลี่ยนแปลงข้อมูลที่ต้องการ
5. คลิก **💾 อัปเดตบทเรียน**

> **หมายเหตุ:** ไม่สามารถเปลี่ยน ID ได้ (เพราะ ID คือตัวระบุตัวตน)
> ถ้าต้องเปลี่ยน ID ให้ลบและสร้างใหม่

---

## วิธีลบบทเรียน

1. ค้นหาบทเรียนในรายการด้านล่าง
2. คลิกปุ่ม **🗑️ ลบ**
3. ยืนยันการลบ

---

## วิธีการ Export (ส่งออก)

เมื่อเตรียมพร้อมจะปรับใช้งาน:

### ขั้นตอนที่ 1: ส่งออก JSON

1. คลิกปุ่ม **📤 ส่งออก JSON**
2. ไฟล์จะถูกดาวน์โหลด:
   - `manifest.json` — รายชื่อบทเรียน
   - `0001-foundation-of-a-disciple.md`, `0002-...`, เป็นต้น — เนื้อหาแต่ละบทเรียน

### ขั้นตอนที่ 2: จัดระเบียบไฟล์

วาง **manifest.json** และไฟล์ **`.md`** ทั้งหมดในโฟลเดอร์ `lessons/`

```
lessons/
├── manifest.json
├── 0001-foundation-of-a-disciple.md
├── 0002-a-servant-leader.md
├── 0003-growing-in-cell-life.md
└── 0004-my-new-lesson.md
```

### ขั้นตอนที่ 3: Deploy

ปรับใช้งานทั้งโปรเจ็คไปยัง:
- GitHub Pages
- Netlify
- Vercel
- FTP server

เว็บไซต์จะโหลด `lessons/manifest.json` อัตโนมัติ

---

## วิธีการ Import (นำเข้า)

ถ้าคุณมี `manifest.json` เก่าที่ต้องการโหลด:

1. คลิกปุ่ม **📥 นำเข้า JSON**
2. เลือกไฟล์ `manifest.json`
3. บทเรียนจะถูกนำเข้าลงใน Admin Panel

> **หมายเหตุ:** นำเข้า manifest.json เพียงจะโหลดข้อมูลพื้นฐาน
> โปรดตรวจสอบเนื้อหาด้วยด้วยการแก้ไขแต่ละบทเรียน

---

## ข้อมูล IndexedDB

ข้อมูลบทเรียนเก็บไว้ในเบราว์เซอร์ (IndexedDB) คุณ

### สำคัญ ⚠️

- **ข้อมูลไม่ sync อัตโนมัติ** — ต้อง export และ upload ด้วยตัวเอง
- **ลบเบราว์เซอร์ cache = ข้อมูลหาย** — ส่งออก JSON เป็นระยะเพื่อความปลอดภัย
- **ต่างเบราว์เซอร์ = ต่างข้อมูล** — ถ้าใช้ Chrome กับ Firefox ข้อมูลจะแยกต่างหาก

### วิธีสำรองข้อมูล

ส่งออก JSON อย่างน้อยทุกสัปดาห์ และบันทึกไฟล์ที่ดาวน์โหลด 
ให้เก็บไว้ที่ปลอดภัย (Google Drive, GitHub, Dropbox เป็นต้น)

---

## Markdown Reference

```markdown
# Heading 1 (ใหญ่สุด)
## Heading 2
### Heading 3

**ตัวหนา** หรือ __bold__
*ตัวเอียง* หรือ _italic_
***ตัวหนา + เอียง***

- bullet list
- item 2
  - nested item

1. numbered list
2. item 2

> blockquote — ใช้สำหรับข้อคิด หรือข้อความพิเศษ

[ลิงก์](https://example.com)

--- (horizontal line)
```

---

## คำถามที่พบบ่อย (FAQ)

### Q: บทเรียนของฉันอยู่ที่ไหน?
**A:** เก็บไว้ใน browser's IndexedDB — ให้เปิด admin.html ด้วยเบราว์เซอร์เดียวกัน

### Q: ต้องทำอะไรเพื่อให้ deploy ไปยัง GitHub Pages?
**A:**
1. Export JSON จากแอดมิน
2. Upload ไฟล์ไปยัง repo
3. Commit และ push
4. GitHub Pages จะเรนเดอร์อัตโนมัติ

### Q: ลบข้อมูลโดยไม่ตั้งใจ ขอบทเรียนคืนได้ไหม?
**A:** ถ้ามีไฟล์สำรอง manifest.json เก่า ให้นำเข้าด้วยปุ่ม "📥 นำเข้า JSON"
ถ้าไม่มี ขอโทษ — ข้อมูลใน browser IndexedDB ไม่สามารถกู้คืนได้

### Q: สามารถใช้ร่วมกับหลายคนได้หรือไม่?
**A:** ในปัจจุบันแต่ละเบราว์เซอร์เก็บข้อมูลแยกกัน
ถ้าอยากใช้ร่วมกัน จะต้องมี backend server (ที่จะเพิ่มทีหลัง)

---

## ขั้นตอนถัดไป

- 🚀 ลองสร้างบทเรียนใหม่ได้เลย
- 📤 Export JSON เพื่อ Deploy
- 🌐 ชม index.html เพื่อเห็นผล
